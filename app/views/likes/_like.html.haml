- if current_user
  - if current_user.already_liked?(@post)
    = link_to "取り消す", post_like_path(@post), method: :delete, remote: true
  - else
    = link_to "イイね", post_likes_path(@post), method: :post, remote: true